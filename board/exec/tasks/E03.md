# E03 — trips.ts structural pass — extract settlement and dispatch (pure refactor)

---
task: E03
round: 3
gate_items: 6,7,9 (enabler)
depends_on: [E02]
migration: no
owns:
  - `apps/api/src/routes/trips.ts`
  - `apps/api/src/lib/settlement.ts`
  - `apps/api/src/lib/dispatch.ts`
---

> **Goal.** Get the money and dispatch logic out of a 1,371-line route file, changing nothing.

## Why this blocks launch

`apps/api/src/routes/trips.ts` is touched by 55 of the 403 planned items and by 6 of the 16 gate items. Until it is split, the trip-lifecycle work and the settlement work cannot run in parallel. T16's P0.2 already proposes the dispatch extraction in exactly this form — a pure refactor in its own PR before any caller changes.

## Scope

- Move the completion/settlement block (`trips.ts:985-1055` at base `b0c0866`) into `apps/api/src/lib/settlement.ts` behind a single exported function. Behaviour identical, including the existing bugs — **do not fix the mint in this task**, E08 owns that.
- Move the dispatch sequence (`trips.ts:518-586`) into `apps/api/src/lib/dispatch.ts`, callable by both the request path and the cron.
- No signature may silently drop a parameter. Diff the extracted block against the original line by line on the PR.

## Acceptance criteria

- `git diff` shows movement, not modification: the settlement and dispatch behaviour is byte-for-byte equivalent in effect.
- `trips.ts` no longer contains balance-moving SQL.
- A reviewer can read the PR and confirm no conditional changed.

## Out of scope

Anything touching a file not in `owns`. If you believe you need one, stop and say so on the PR — do not
edit it. Another chat owns it and your change will be silently lost or will lose theirs.

## Definition of done

1. One PR into `main`, small enough to read in one sitting.
2. If this brief names tests, they exist and run in CI. **If it names none, say so in the PR and
   stop there** — do not invent a test harness. `ci.yml` runs `flutter analyze` and never
   `flutter test`, so a Dart test you add today runs nowhere; wiring that up needs a workflow edit
   no agent can make. Propose it in the PR body and leave it to a human (see WAVE-PLAN §9).
3. You do **not** merge it yourself.
4. After merge, a different chat verifies the *effect* against `main` and closes the finding. You move it
   to `awaiting-verification` and stop.
