---
task: E10
title: Rider client — active-trip recovery and the discarded 409 tripId
status: done
claimed_by: chat-20260802-0726-6f1e
claimed_at_utc: 2026-08-02T07:50:19Z
branch: exec/10-rider-active-trip-recovery
pr: https://github.com/synapticadmin-del/-godrive/pull/107
owns:
  - apps/rider/lib/services/app_state.dart
  - apps/rider/lib/screens/home/fare_estimate_sheet.dart
  - apps/rider/lib/main.dart
migration: no
finished_at_utc: 2026-08-02T08:00:32Z
---

## Progress
- claimed 2026-08-02T07:50:19Z. `depends_on: [E09]` confirmed **merged into `main`**, not merely
  `done` — PR #105. E10 is the last unclaimed execution task and the whole of round 6.
- LOCKED at claim time = **{}** — all 21 existing claims are `status: done`, so there is no
  `in_progress` `owns` set to intersect. No file lock blocks this task.
- Board state at claim: E01–E09, E11–E20 all `done`. PRs merged: #88–#100, #101, #102, #103, #105,
  #106. **#104 (E07) was still open at claim time** — irrelevant to E10, which does not depend on it.
- Operational note carried forward from the prior run: `raw.githubusercontent.com` is CDN-cached and
  serves stale content immediately after a write. The §3.4 read-back used the **contents API**.
- re-read the claims folder immediately before opening the PR per §3.5: only this claim was
  `in_progress`, no intersection with anyone. Base `main` moved from `696e258f` to `a95efeb0`
  mid-run; all three owned files were re-fetched and confirmed byte-identical across the move
  before pushing.
- **done** — PR #107, `awaiting-verification`. No PR merged by me, no finding closed by me.

## What was actually done

1. `bootstrap()` now calls `GET /trips/active` (E09's endpoint, which had no caller on `main`) and
   caches the result; `main.dart` pushes `TripScreen` on top of `HomeScreen` via a navigator key,
   one-shot and gated on the splash having finished. Force-quitting mid-trip reopens into the trip.
2. `ApiException` preserves the server's structured body through the transport layer, so the
   `409 ACTIVE_TRIP`'s `tripId` survives. `toString()` keeps the `Exception: ` prefix, verified
   against all five display sites in the rider app so no existing error message changes.
3. All five HTTP helpers now throw through one `_throwApiError`; the duplication across five copies
   is why the id was dropped on exactly the path that needed it.
4. The fare sheet offers the stuck trip in a dialog instead of a dead-end snackbar. Copy is inline
   bilingual, not `app_strings.dart` (§4.1 consequence file, not owned).
5. **Seam #13 reported, not touched.** `accepted_price` (F-09-02) is rendered in
   `trip_screen.dart:623` and `:651`, which is **E20's** file. Two of three scope bullets are
   delivered; the fare bullet cannot be done from this task's `owns:` and no live task owns it.
   No dead helper was added to fake it — that would be root R3.

## Not closed by me
- **Gate item 7 is a split (E09 + E10) and this PR is the recovery half only.** The in-ride fare is
  still wrong until seam #13 has an owner. A verifier closing item 7 on #107 closes it wrong.
- `flutter analyze` was **not** run — no Dart toolchain and no registry access in the sandbox, the
  same 403 V01, V02, E07 and E19 all recorded. Verified structurally instead (balanced-delimiter
  scan, full diff audit showing the only deletions are the five throw statements, and a byte-for-byte
  read-back of the pushed branch).
