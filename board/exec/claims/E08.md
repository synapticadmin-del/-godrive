---
task: E08
title: "Settlement correctness: accept settles offered_price, cash floor, one money primitive"
status: done
claimed_by: chat-20260802-0506-3804
claimed_at_utc: 2026-08-02T05:06:40Z
branch: exec/08-settlement-money-primitive
pr: https://github.com/synapticadmin-del/-godrive/pull/102
owns:
  - apps/api/src/lib/settlement.ts
  - apps/api/src/lib/money.ts
migration: no
finished_at_utc: 2026-08-02T05:28:00Z
---

## Progress
- claimed — deps verified merged on main: E03 (PR #97, merged), E12 (PR #98, merged).
- LOCKED at claim time = E04 {lib/schemas.ts, routes/payments.ts, routes/intercity.ts, routes/companies.ts} + E14 {routes/admin.ts, apps/admin/}. No intersection with this task's owns.
- Added `apps/api/src/lib/money.ts`: one `moveMoney()` implementing the insert-as-lock idiom correctly (INSERT OR IGNORE against unique `idx_wt_idem` is the lock, balance UPDATE gated on EXISTS, both in one `db.batch()`), with an integer-piastre balance floor enforced inside the insert's WHERE. Root R1's three hand-rolled moves inside `settlement.ts` are gone; the two in `wallet.ts`/`payments.ts` are other tasks' files and R1 is not fully closed.
- Rewrote `apps/api/src/lib/settlement.ts` onto it. `resolveTripSettlement()` puts `trips.offered_price` in the price chain (written at `trips.ts:409`, read nowhere on main — T05 F-05-01) and rescales commission at the booking's own rate so the captain is credited *from* the agreed price. Cash commission debit gained the missing floor (F-03-11/F-04-12, G1). `logAudit(critical: true)` now called on the completion path (F-22-05).
- Wallet path left G1‡ — quarantined by E04's enum narrowing, not redesigned. One exception, stated on the PR and in the PROJECT.md block: the captain's credit is withheld when the rider's debit did not settle, because leaving a live money-minting path behind an enum is not the same as failing closed.
- Proof: `board/exec/drafts/e08_settlement_harness/` runs the shipped files against real SQLite with all 22 migrations through a D1 shim — **69 assertions, 0 failures**, including a live replay of the old rider debit double-charging on retry (500 → 400 → 300). `tsc` 5.8.3 under `apps/api/tsconfig.json`: **0 errors**, including a call-site contract proving `routes/trips.ts` still compiles untouched. `check_repo_hygiene.py`: OK.
- Tests: the brief names one, but `apps/api/test/` is E19's `owns:` and E19 `depends_on: [E01, E08]`. Runner-shaped suite parked at `board/exec/drafts/e19_adopt_settlement.test.ts`. **E19 is unblocked by this PR.**
- **Gate item 6 spans E08 and E09 — not closable on PR #102.** `trips.ts:873` still passes its own number and belongs to E09; disagreements increment `settlement_price_mismatch` so the remaining gap is measurable on main.
- Seam 13 reported: `apps/api/src/lib/types.ts` is unowned and `DbTrip` lacks `offered_price`; declared locally as `SettlementTrip` rather than editing an unowned file.
- Brief line drift recorded: accept handler is `trips.ts:733` not `:829`; the cash commission debit cited at `trips.ts:1029-1033` was moved into `settlement.ts` by E03.
- §3.5 re-checked before opening the PR: no other claim locks either file. PROJECT.md block appended to main at `a6248db`.
- Not merged by me, and no finding closed by me — `awaiting-verification`, per PROTOCOL-EXEC §7.
