---
task: E21
title: Rider in-ride fare shows the agreed price, not the system estimate
status: done
claimed_by: chat-20260802-0726-6f1e
claimed_at_utc: 2026-08-02T08:11:09Z
branch: exec/21-inride-agreed-fare
pr: https://github.com/synapticadmin-del/-godrive/pull/109
owns:
  - apps/rider/lib/screens/trip/trip_screen.dart
migration: no
finished_at_utc: 2026-08-02T08:15:22Z
takeover_of:
---

## Provenance — read this before objecting to the task number

**E21 did not exist in the original 20. It was created at the board owner's explicit instruction**
after E10 (PR #107) reported **seam #13**, exactly as **E20 was created post-round-1** for the
missing second half of gate item 8 (see `WAVE-PLAN.md` §10, `chat-20260801-1910-a4e5`, "at the
board owner's instruction"). The gap is the same shape: a gate item whose *effect* lands in a file
that no live task owns.

**Same chat, second task — declared, not hidden.** `chat-20260802-0726-6f1e` already holds E10
(`done`, PR #107). PROTOCOL-EXEC assumes one chat per task and E10's own PROJECT.md block flagged
`chat-20260801-2003-b678` for silently holding both E02 and E14. Minting a fresh CHAT_ID here would
disguise the same pattern; reusing it and saying so is the disclosure that case lacked. §7's
"a **different** chat verifies" therefore excludes this chat from verifying **both** E10 and E21.

## Lock check

- LOCKED at claim time = **{}**. All 22 existing claims are `status: done`; no claim is
  `in_progress`, so no `owns` set to intersect.
- `apps/rider/lib/screens/trip/trip_screen.dart` is listed in `WAVE-PLAN.md` §8 under **E20**,
  whose claim is `done` and whose PR **#95 is merged**. A `done` claim contributes nothing to
  LOCKED under §3.1, so the file is unlocked — but it was also **unowned by any live task**, which
  is precisely why E10 could not fix it and had to report instead.
- PR **#108** (E00 human duties) was checked before claiming: workflows, `requestId` activation,
  the ci.yml test step and `tsconfig` `test/**/*` — 6 files, **none of them this one**. No collision.

## Progress
- claimed 2026-08-02T08:11:09Z. Brief written to `board/exec/tasks/E21.md` in the same pass.
- re-read claims before pushing per §3.5: 22 others all `done`, only this one `in_progress`.
- **done** 2026-08-02T08:15:22Z — PR #109, `awaiting-verification`. No PR merged by me, no finding closed by me.

## What was actually done

1. `trip_screen.dart:623` and `:651` read `estimated_fare` in panels that only render **after** a
   captain accepts, so `accepted_price` always existed and was always ignored (F-09-02). Both now
   read the agreed price.
2. One `_displayFare()` helper defines the precedence once — it was written three divergent ways
   across `:623`, `:651` and `:671`. Live trip prefers `accepted_price`; completion still prefers
   `final_fare`, since settlement may adjust for waiting time or tolls.
3. **Exactly three lines removed**, verified by diff audit; the rest of the +34 is the helper and
   its rationale. No widget, layout, label or network call touched.
4. Board amended as the owner asked: gate item **7 widened to E09 + E10 + E21** in §4 with the split
   explained in prose, **seam #13** recorded in §7 (and "twelve" corrected to thirteen), §8 ownership
   map and the §1 round-6 row updated, §10 changelog entry added.

## Not closed by me
- **Gate item 7 needs all three of #105, #107, #109 merged and verified.** #105 is merged; the other
  two are open. A verifier closing item 7 on any one of them closes it wrong.
- **This chat may verify neither E10 nor E21** — §7 requires a different chat, and this chat authored
  both. Declared rather than disguised with a fresh CHAT_ID.
- `flutter analyze` **not** run — no Dart toolchain, no registry access (the same 403 five prior
  claims recorded). Verified structurally: balanced-delimiter scan (1,023 lines), full diff audit,
  byte-for-byte read-back of the pushed branch.
