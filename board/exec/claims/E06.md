---
task: E06
title: Freeze the payout debit
status: done
claimed_by: chat-20260801-1840-091b
claimed_at_utc: 2026-08-01T18:42:10Z
branch: exec/06-freeze-payout-debit
pr: https://github.com/synapticadmin-del/-godrive/pull/90
owns:
  - apps/api/src/routes/wallet.ts
  - migrations/0020_payout_requests.sql
migration: yes — 0020, taken from MIGRATION-LOCK.md (E16 took 0021 after, no collision)
finished_at_utc: 2026-08-01T19:02:00Z
---

## Progress
- claimed
- verified both cited defects on `main` before changing anything: `wallet.ts:113-117` debits
  `users.wallet_balance` at request time, `:124-130` records it as a `status='pending'`
  wallet_transactions row with no `idempotency_key` despite 0005 adding the column.
- took migration number 0020 from `board/exec/MIGRATION-LOCK.md` with its sha; renamed
  `@NEXT_payout_requests.sql` to `0020_payout_requests.sql` in `owns` above.
- migration 0020 adds `payout_requests` (three reachable states, four CHECKs incl. a partial
  unique index scoped to open requests). No backfill — the stranded rows stay put and are
  enumerated by a query in the PR body.
- `wallet.ts`: the request records and reserves and moves no money; `settlePayoutRequest()` is
  the only payout debit path and is what E14 calls. Follows the house idiom
  (`INSERT OR IGNORE` on an idempotency key then move the balance) inside a `db.batch()`.
- proof: 40/40 assertions against migrations 0001–0020 on a fresh SQLite db, driving the exact
  SQL from `wallet.ts`; `check_migrations.py` and `check_migrations_apply.py` both pass.

## Reported on the PR, not fixed by me
- **Seam 13.** The brief demands a test in CI but every path one could live in is owned by
  E19 (`apps/api/test/`, `vitest.config.ts`), E01 (`apps/api/package.json`) or nobody
  (`scripts/`, `ci.yml`). Harness shipped in the PR body for E19 to adopt. Did not reach.
- **Gate item 4 is a fourth split item, missing from WAVE-PLAN §4.** `E14.md` also carries
  `gate_items: 10,4`. This PR is the definition side only; the item must not be closed until
  E14 merges. Left as awaiting-verification.

## Not done, deliberately
- did not merge, did not close S-003 / S-006 / S-020 — all three moved to awaiting-verification.
