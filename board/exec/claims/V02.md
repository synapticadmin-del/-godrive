---
task: V02
verifies: E02
title: Verification — index.ts structural pass, its four acceptance criteria and its three reserved seams, against main
status: done
claimed_by: chat-20260802-0542-7460
claimed_at_utc: 2026-08-02T05:42:45Z
branch: —
pr: — (verification produces no branch; results posted on PR #94 and PR #98)
owns: []
migration: no
finished_at_utc: 2026-08-02T05:46:00Z
---

## Progress
- claimed. Verified E02 (PR #94, merged) against `main` at `79c0456`. Author summary deliberately not
  read before forming a view (PROTOCOL-EXEC §7). Not the author of E02.
- posted on PR #94 (comment 5155731908) and PR #98 (comment 5155732239); PROJECT.md entry on `main` at `69bc853`.
- done. No PR merged by me, no finding closed by me.

## Result

**E02's four acceptance criteria: PASS.** Cron is one file per job (`invoices.ts`, `dispatch.ts`,
`cleanup.ts`) behind a dispatcher that throws on an unregistered schedule (`scheduled.ts:67-70`) and
rethrows collected per-job failures (`:79-89`) — a failed job can no longer be recorded as a successful
invocation. `/intercity` and `/companies` have no mount. The public `/safety/track/*` shim
(`index.ts:203`) precedes `app.route("/safety", …)` (`:243`).

**Both late-bound route seams are live.** Worth checking precisely because an unresolved shim fails
silently — the request falls through to the authenticated router and 401s, indistinguishable from normal
behaviour. On `main`: `routes/user.ts:771` exports `publicUserRoutes` with `POST`/`GET
/deletion-request`; `routes/safety.ts:228` exports `publicSafetyRoutes` with `GET /track/:token`. Both
match the contract at `index.ts:151-156`. That is E02's half of gate items 10 and 12.

**FAIL — gate item 14 is not delivered.** `middleware/requestId.ts` is on `main` (E12, PR #98 merged)
exporting `requestId()` `:119`, `getRequestId()` `:94`, `getLogger()` `:105`. Nothing imports or calls any
of them; `index.ts:61-63` is still E02's no-op passthrough and the only occurrence of `requestId` in that
file is a comment. No request gets an id, no `X-Request-Id` header is emitted, so E12's headline criterion
is false. Root R3: defined, never called.

Not E02's mistake — `index.ts:53-60` predicted it and asked for an explicit one-line exemption to the
freeze, which was never granted. Activation is two lines in a file no Wave-1 task may edit, so it belongs
to the freeze-holder under **E00**.

E12's other three criteria hold: `/health` really probes D1/KV/R2 and returns 200/503 naming the culprit;
`audit.ts:101` increments `audit_write_failed`; `pingDeadMan()` (`log.ts:255`) is legitimately uncalled
until E07 and E09 ship their call sites.

## Also established

- CI `checks` job run against `main`: `check_migrations` OK (22), `check_migrations_apply` **OK, 22/22 on a
  fresh database, 40 tables**, `check_l10n_parity` OK, `check_repo_hygiene` OK (345 files). This settles the
  open question in `MIGRATION-LOCK.md` §4 — `0020`, `0021` and `0022` do apply together. Never executed
  anywhere before this run.
- **The `node` CI job could not be run.** `registry.npmjs.org` is blocked from the sandbox (direct `curl`
  403s for `typescript` too, so an egress restriction, not a lockfile fault). Twelve tasks have merged into
  `main` and nobody has typechecked the result; branch protection is off so no required check has run
  against `main` either. Flagged rather than reported as green.

## Still owed

- The `index.ts` freeze exemption (gate item 14) — **E00**.
- A typecheck of `main` by someone with a working registry.
- V02 is the first `VNN` claim on this board. E01, E03, E05, E06, E11, E13, E16, E17, E18 and E20 are all
  merged and still unverified.
