---
task: E01
title: Deploy safety net — no more bare wrangler deploy
status: done
claimed_by: chat-20260801-1830-ee41
claimed_at_utc: 2026-08-01T18:31:22Z
branch: exec/01-deploy-safety-net
pr: https://github.com/synapticadmin-del/-godrive/pull/89
owns:
  - apps/api/package.json
  - apps/api/deploy.sh
  - docs/DEPLOYMENT.md
  - package-lock.json
migration: no
finished_at_utc: 2026-08-01T18:53:00Z
---

## Progress
- claimed
- 2026-08-01T18:38Z — brief updated by the board owner: `package-lock.json` added to `owns`.
  Mirrored here. Reason: npm-workspaces repo with a single root lockfile, and `ci.yml` runs
  `npm ci`, which fails when the lockfile has drifted from the manifests. Reproduced that
  failure locally before the brief changed, so the lockfile is regenerated in the same commit.
- 2026-08-01T18:46Z — second brief update: added `"test": "vitest run"` to apps/api, and read
  the plan from `plan/29-execution-plan` (it is not on `main`).
- 2026-08-01T18:53Z — done. PR #89 open, PROJECT.md block appended to `main`.

## What I actually did

1. **Rewrote `apps/api/deploy.sh`.** The environment is a required positional argument,
   validated against the `[env.*]` blocks parsed out of `wrangler.toml` (not hardcoded, so it
   cannot drift). The D1 name is read from `[[env.<env>.d1_databases]]` and the script stops
   rather than guessing if it is absent. Migrations now run through
   `wrangler d1 migrations apply --remote --env <env>` — all pending, each recorded — replacing
   a hardcoded single-file `d1 execute` that had no `--remote` and so hit the LOCAL database
   while the deploy two lines later went to production.
2. **`apps/api/package.json`:** `deploy` → `bash ./deploy.sh` (refuses with no env, exits 1);
   added `deploy:prod`, `deploy:staging`, `test`, `test:deploy`; added
   `@cloudflare/vitest-pool-workers ^0.12.21` for E19; `db:migrate:remote` gained `--env prod`
   (behaviour-neutral — same `database_id` — and disclosed on the PR as the one change not
   literally required by the brief). The root `package.json` is untouched: `deploy:api` is a
   pure delegation, so fixing the leaf satisfies the acceptance criterion without reaching
   across the ownership boundary.
3. **Regenerated `package-lock.json`** with `npm install --package-lock-only`, never by hand.
   391 → 432 entries: 41 added, **0 removed, 0 re-resolved**, 1 modified (the intended
   `apps/api.devDependencies`). `npm ci` exits 0 (276 packages) and `npm test -w
   @synaptic-go/shared` still passes 41 tests.
4. **Corrected `docs/DEPLOYMENT.md`.** Withdrew the "17 applied, nothing pending" claim — the
   repo has 19 migrations and nothing here can observe the remote DB. Remote state is recorded
   as unknown, with the single command that establishes it and a pointer to E00's block as its
   only writer.
5. **Tests:** `npm run test:deploy` → 17 assertions, 0 failures, no wrangler/credentials/network.
   Mutation-tested against all three original defects (1, 4 and 3 failures respectively). Two
   bugs in the suite were found that way and fixed. Not wired into CI — no agent can write
   `.github/workflows/**`; the step is in PR #89 for the same human pass as E00.

## Flags for the verifier

- **Do not close gate item 2 on PR #89.** It is split between E00 (human: install the workflow)
  and E01 (this PR: kill the bare deploy) — the item's own wording says so, but WAVE-PLAN §4
  does not list it alongside items 6/10/11. The deploy path is now safe but still manual.
- **Seam closed mid-run:** `package-lock.json`, ruled by the board owner into E01's `owns`.
- **Seam still open:** `apps/api/tsconfig.json` has `include: ["src/**/*", ...]`, which does not
  cover `apps/api/test/` where E19's tests land; E19 does not own `tsconfig.json`.
- **Scheduling consequence:** `package-lock.json` now has an owner, so no other task in this
  wave can add an npm dependency — manifest and lock must move together.
