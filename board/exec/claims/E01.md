---
task: E01
title: Deploy safety net — no more bare wrangler deploy
status: in_progress
claimed_by: chat-20260801-1830-ee41
claimed_at_utc: 2026-08-01T18:31:22Z
branch: exec/01-deploy-safety-net
pr:
owns:
  - apps/api/package.json
  - apps/api/deploy.sh
  - docs/DEPLOYMENT.md
  - package-lock.json
migration: no
finished_at_utc:
---

## Progress
- claimed
- 2026-08-01T18:38Z — brief updated by the board owner: `package-lock.json` added to `owns`.
  Mirrored here. Reason: npm-workspaces repo with a single root lockfile, and `ci.yml` runs
  `npm ci`, which fails when the lockfile has drifted from the manifests. Reproduced that
  failure locally before the brief changed, so the lockfile is regenerated in the same commit.
- read the three owned files at base `f480905deb2db331d9087594ccd285029ba61660`; verified both
  cited defects: `apps/api/package.json:8` is a bare `wrangler deploy`, `apps/api/deploy.sh:40`
  applies a single hardcoded migration via `wrangler d1 execute --file=`, `:47` deploys with no `--env`.
