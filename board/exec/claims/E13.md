---
task: E13
title: Safety API — redact the share payload, fix the public route, SOS lifecycle
status: done
claimed_by: chat-20260801-2104-eb59
claimed_at_utc: 2026-08-01T21:05:00Z
branch: exec/13-safety-share-redaction-sos-lifecycle
pr: https://github.com/synapticadmin-del/-godrive/pull/99
owns:
  - apps/api/src/routes/safety.ts
  - migrations/0022_sos_lifecycle.sql
migration: yes — 0022, the row already reserved for E13 in board/exec/MIGRATION-LOCK.md (no new number taken)
finished_at_utc: 2026-08-01T21:25:00Z
---

## Progress
- 2026-08-01T21:05Z — claimed. Base `main` pinned at `f58f22742ebaa20455fc82eb9e4569ecfedc42db`
  (E02 / PR #94 merged 20:55:01Z, this task's only `depends_on`).
- LOCKED at claim time (union of `owns` over in_progress claims E03 + E12):
  `routes/trips.ts`, `lib/settlement.ts`, `lib/dispatch.ts`, `middleware/requestId.ts`,
  `lib/log.ts`, `lib/audit.ts`, `lib/health.ts`. No intersection with this task's `owns`.
- 2026-08-01T21:18Z — implementation done; 47/47 behaviour assertions green; all three CI
  check scripts pass locally.
- 2026-08-01T21:20Z — §3.5 re-read of claims. E03 and E12 flipped to `done` while this ran;
  nobody has taken `routes/safety.ts` or `migrations/0022_sos_lifecycle.sql`. No collision.
- 2026-08-01T21:25Z — done. PR #99 open. Branched from `5c9969a2` (main moved two commits
  mid-run; both `PROJECT.md` appends, no product file differs).
- Finding state: **awaiting-verification**. Not merged by me, not closed by me.

## What I actually did

1. **Redacted the tracker payload, in the same commit as the URL fix** (WAVE-PLAN §6). `pickup_address`
   and `dropoff_address` are gone; the position is coarsened to 3 dp (~110 m). Fixing the URL first
   would have published the rider's home address to an unauthenticated token with a 7-day life.
2. **Exported `publicSafetyRoutes` with `/track/:token`**, matching E02's contract at `index.ts:203`
   exactly, and removed the handler from the authenticated router so the public mount is the only
   route to it. Liveness derives from the shared `TRIP_TRANSITIONS` machine and **fails closed**:
   an unknown status discloses no position, so E09's new terminal state needs no edit here.
3. **SOS lifecycle** on an append-only trail enforced by trigger: queue, detail+history, ack
   (guarded so a second operator cannot overwrite the first), escalate/contact/note, and resolve
   (guarded, reason mandatory, actor recorded). No new `status` value — `0003:170`'s CHECK is intact.
4. **Adopted `0022`** from `board/exec/drafts` verbatim but for one comment correction: the draft said
   `created_at` was indexed alone; `0003:174` also creates `idx_sos_status`. The composite is still right.
5. **Exported `revokeShareToken()` / `purgeExpiredShareTokens()` and wired neither** — both call sites
   are E09's files. Said so on the PR so no verifier closes item 10 on this alone.

## Two things the next chat needs

- **`0022` MUST NOT merge before `0021` (E16, PR #92).** `check_migrations.py:134-141` fails on a *gap*.
  Merging this first turns a required check red **on `main`**, not just on the PR. Proved both ways.
  This edge is in no `depends_on` and `validate.py` cannot see it — it compares `owns` paths, and two
  differently-numbered migration files never intersect. Same trap waits for `0023`/E15.
- **A seam, unresolved by design.** This brief names four tests; their only legal home is
  `apps/api/test/` + `vitest.config.ts`, which are E19's `owns:` (round 5, blocked). `ci.yml` also
  never invokes the api workspace's test script, so a suite added today runs nowhere. Behaviour is
  proved by `board/exec/drafts/test_e13_safety.py` (47 assertions) and the runner-shaped suite is
  parked inert at `board/exec/drafts/e19_adopt_safety.test.ts`. Nothing outside `owns` is on the PR.
