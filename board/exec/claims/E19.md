---
task: E19
title: Tests on the money paths that stay live
status: in_progress
claimed_by: chat-20260802-0722-9ced
claimed_at_utc: 2026-08-02T07:25:18Z
branch: exec/19-money-path-tests
pr:
owns:
  - apps/api/test/
  - apps/api/vitest.config.ts
migration: no
finished_at_utc:
---

## Progress
- claimed. `depends_on: [E01, E08]` both confirmed **merged into `main`** via the pulls API
  (`merged: true`): E01 = PR #89, E08 = PR #102. Not merely `done`.
- LOCKED at claim time = `{apps/api/src/cron/invoices.ts}` (E07, `chat-20260802-0714-6e72`) ∪
  `{apps/api/src/routes/trips.ts, apps/api/src/lib/dispatch.ts, apps/api/src/cron/dispatch.ts,
  apps/api/src/lib/cleanup.ts, apps/api/src/durable-objects/OfferScheduler.ts}` (E09,
  `chat-20260802-0722-0f3e`). No intersection with this task's `owns`.
- Reached for E09 first (lower-numbered, deps merged); the unconditional create was rejected because
  `chat-20260802-0722-0f3e` had taken it 85 seconds earlier. The §3.3 lock worked. Fell through to E19.
- Base `main` pinned at `abbb95f8126e7f68a5f0703bbdcd15900eda2d7f`.
- Known seam ahead, per WAVE-PLAN §8: `apps/api/tsconfig.json` has `include: ["src/**/*"]`, which does
  not cover `apps/api/test/`, and the file is owned by nobody. Will not reach for it — will report.
