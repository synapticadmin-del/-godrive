---
task: E19
title: Tests on the money paths that stay live
status: done
claimed_by: chat-20260802-0722-9ced
claimed_at_utc: 2026-08-02T07:25:18Z
branch: exec/19-money-path-tests
pr: https://github.com/synapticadmin-del/-godrive/pull/106
owns:
  - apps/api/test/
  - apps/api/vitest.config.ts
migration: no
finished_at_utc: 2026-08-02T07:41:55Z
---

## Progress
- claimed. `depends_on: [E01, E08]` both confirmed **merged into `main`** via the pulls API
  (`merged: true`): E01 = PR #89, E08 = PR #102. Not merely `done`.
- LOCKED at claim time = `{apps/api/src/cron/invoices.ts}` (E07, `chat-20260802-0714-6e72`) ∪
  `{apps/api/src/routes/trips.ts, apps/api/src/lib/dispatch.ts, apps/api/src/cron/dispatch.ts,
  apps/api/src/lib/cleanup.ts, apps/api/src/durable-objects/OfferScheduler.ts}` (E09,
  `chat-20260802-0722-0f3e`). No intersection with this task's `owns`.
- Reached for E09 first (lower-numbered, deps merged); the unconditional create was rejected because
  `chat-20260802-0722-0f3e` had taken it 85 seconds earlier. The §3.3 lock worked. Fell through to E19.
- Base `main` pinned at `abbb95f8126e7f68a5f0703bbdcd15900eda2d7f`; branched from
  `80eb0aea2972f28bfee6a678a0c7132c81e5f395` after E07 appended its PROJECT.md block (that commit
  touched PROJECT.md only — verified).
- Known seam ahead, per WAVE-PLAN §8: `apps/api/tsconfig.json` has `include: ["src/**/*"]`, which does
  not cover `apps/api/test/`, and the file is owned by nobody. Will not reach for it — will report.

## Done — PR #106

1. **Stood up `@cloudflare/vitest-pool-workers`** in `apps/api/vitest.config.ts`: tests run inside
   `workerd` against the real `wrangler.toml` bindings, with the repository's own `migrations/*.sql`
   read at config time rather than a fixture, and `isolatedStorage` on.
2. **Wrote the four tests the brief names** — cash-commission settlement incl. the balance floor;
   completion idempotency (the row guard *and* `idx_wt_idem`, separately, because the 409 is the cheap
   half); the two-captain accept race; and E04's payment-method rejections. No test touches a third
   party: `OSRM_URL` is unroutable, and the one valid-body test stops at `NO_PRICING` before routing.
3. **The suite has NOT been executed** — the sandbox gets `403` from `registry.npmjs.org`, so no
   `npm ci` and no `workerd`. Said so plainly in the PR title and body. Gate item 16 must **not** be
   closed on this PR; it closes on a green CI run URL. Anything else is root R3.
4. **Shipped dependency-free evidence** at `apps/api/test/tools/offline-verify.mjs`: drives the real
   source and the real 23 migrations (23/23, 40 tables — matches MIGRATION-LOCK §3) on `node:sqlite`
   with zero installs. 17/17 green, and `--revert=floor|completion|race|enum` turns each guarantee red
   on demand — `--revert=floor` drives a captain's wallet to **−15 EGP**, the bug as it behaved. Built
   for the §7 verifier so it need not trust my summary or wait for CI.
5. **Reported two seams, touched neither.** (a) **New, not in WAVE-PLAN §7's twelve:** `vitest` is
   undeclared in `apps/api/package.json` and resolves only via workspace hoisting from
   `packages/shared`, against a narrow peer range — E01's file, and the fix must regenerate the
   lockfile in the same commit. (b) The predicted `apps/api/tsconfig.json` seam: `include` stops at
   `src/`, so `npm run typecheck` never sees the suite; added `test/tsconfig.json` inside my `owns`
   for editors, which does not close it. CI YAML went to `docs/plan/assets/E19-ci-test-job.yml`.

Finding status: **awaiting-verification.** Not merged, not closed, by me.
