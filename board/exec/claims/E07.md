---
task: E07
title: Disable the B2B invoice cron and collapse to one generator
status: done
claimed_by: chat-20260802-0714-6e72
claimed_at_utc: 2026-08-02T07:14:26Z
branch: exec/07-invoice-cron-shutdown
pr: https://github.com/synapticadmin-del/-godrive/pull/104
owns:
  - apps/api/src/cron/invoices.ts
migration: no
finished_at_utc: 2026-08-02T07:36:00Z
---

## Progress
- claimed — deps verified **merged on main**, not merely done: E02 (PR #94, merged), E04 (PR #100, merged), E12 (PR #98, merged).
- LOCKED at claim time = {} — all 18 existing claims were `status: done`. Base `main` pinned at `abbb95f8126e7f68a5f0703bbdcd15900eda2d7f` (E15, PR #103).
- **Verified the finding before building on it.** F-08-01 reproduced against the repo's own 23 migrations in SQLite using the SQL copied byte-for-byte from `main`: the `SUM` wraps both bounds in `datetime()`, the settling `UPDATE` compares raw strings, and `trips.created_at` is the table default `'YYYY-MM-DD HH:MM:SS'` (`0001_init.sql:80`; the insert at `trips.ts:405` never binds it) against a bound `toISOString()`. A space sorts before `'T'`, so first-day trips were never settled and were re-invoiced every run — 3 runs produced 3 invoices totalling 550.00 against 350.00 of real trips.
- Shipped one file, `apps/api/src/cron/invoices.ts`: the job is off behind a **contiguous guard that must be deleted, not toggled** (no flag, no env var); it stays *registered* so a monthly `cron.heartbeat` still proves the cron plumbing is alive (F-22-03); `pingDeadMan()` is called on the success path in both states; and the generator is collapsed to one `generateCompanyInvoice()` — unreachable — whose two statements finally agree on the period, with `previousUtcMonth()` fixing the local-time constructor and the `new Date()` upper bound that swept the new month's first hours into the old month's invoice.
- Proof: **31 assertions, 0 failures.** 14 reproduce the defect; 17 execute the *shipped* file itself (Node 24 type-stripping, real `lib/log.ts` + `lib/utils.ts`, real SQLite + 23 migrations, D1 shim) — disabled job writes 0 invoices and mutates 0 flags, heartbeat still emitted with `billing=disabled`, and the collapsed generator issues 1 invoice for 350.00 across three runs instead of 3 for 550.00. `check_repo_hygiene.py` / `check_migrations.py` / `check_migrations_apply.py`: all **OK**. Harness parked for E19 at `board/exec/drafts/e19_adopt_invoices.mjs`.
- `tsc` could **not** be run — no git credentials and `registry.npmjs.org` 403s, so `npm ci` is impossible in the sandbox. Said so on the PR rather than claiming a check I did not perform. No new dependency, no manifest touched.
- **Gate item 5 is half — not closable on PR #104.** My brief says the `companies.ts:200` mirror generator (F-08-20, cited as `:167` before E04's guard shifted it) is dead by E04's shutdown, not by my edit. But **`WAVE-PLAN.md` §4 lists item 5 as a single-closer (`5 → E07`)**, which contradicts the brief; §4 asks for a row to be added, but that file is outside my `owns:` so I named the gap on the PR instead of editing it. **Needs a row: item 5 → `E07` + `E04`.**
- Tests: the brief **names none**, so none were added — stated explicitly on the PR per DoD §2 and WAVE-PLAN §9. `apps/api/test/` is E19's `owns:` and `ci.yml` never runs the API suite.
- Left deliberately: the money units. The generator still sums the legacy `REAL` columns while `0005` made piastres the source of truth — switching today would bill **zero**, because `trips.ts:878` still writes `final_fare` and not `final_fare_piastres`. Named as a re-enable precondition, not smuggled into a shutdown. `UNIQUE(company_id, period_start)` is wave 2; no migration number taken.
- No new seam. The E12→E07 `pingDeadMan()` export seam worked as designed and E04's shutdown covered the other half without my touching the file.
- §3.5 re-checked before opening the PR: **E09 was claimed at 07:22:53Z** by `chat-20260802-0722-0f3e`; its `owns:` does not intersect `cron/invoices.ts`. No conflict.
- Board note: **E14's claim is `done` but PR #101 is still open** — gate items 4 and 10 are not closable and anything depending on E14 is blocked by §3.2.
- Not merged by me, no finding closed by me. F-08-01 is `awaiting-verification` per PROTOCOL-EXEC §7.
