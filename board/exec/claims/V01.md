---
task: V01
verifies: E01
title: "Verification — E01 deploy safety net (gate item 2), against main"
status: done
claimed_by: chat-20260802-0548-f769
claimed_at_utc: 2026-08-02T05:49:30Z
branch: —
pr: — (verification produces no branch; result posted on PR #89)
owns: []
migration: no
finished_at_utc: 2026-08-02T05:57:00Z
---

## Progress
- claimed. Taken because **no execution task is unblocked**: E07 waits on E04 (PR #100 open),
  E09 on E08 (#102) and E15 (#103), E19 on E08 (#102), E10 on E09 (no claim). LOCKED was empty
  — no claim is in_progress — so nothing was blocked by a file lock, only by unmerged deps.
- E01 (PR #89) is the lowest-numbered merged-and-unverified task. Not the author of E01.
- Per §7 the author's summary is deliberately not read before forming a view.
- Verified against `main` @ `69bc853`. Posted on PR #89 (comment 5155781080); PROJECT.md entry
  on `main` at `35d0592`.
- done. No PR merged by me, no finding closed by me.

## Result

**E01's four acceptance criteria: PASS**, proven by execution rather than by reading.

1. `npm run deploy:api` with no environment was run through the **real npm chain** and exits `1`,
   refusing via the guard and emitting **no `wrangler` command at all**. A script that exited
   non-zero after calling `wrangler deploy` would still have deployed; this one never reaches it.
2. All 28 script/config files on `main` scanned: no live hardcoded migration anywhere. Every
   `d1 execute` hit is a comment, a self-test literal, or documentation of the old behaviour. The
   only one that executes is `scripts/backup-d1.sh:885`, read-only, and E18's. `prod --dry-run`
   applies all pending migrations *before* deploying; `staging` targets `synaptic-go-staging`.
3. `@cloudflare/vitest-pool-workers ^0.12.21` present in the manifest and in the root lockfile
   (40 entries). `npm ci --dry-run` resolves cleanly — 276 packages, **no `EUSAGE` sync error**.
   A real `npm ci` clears sync validation and then 403s at tarball fetch; an independent `curl`
   to `registry.npmjs.org` also 403s. Sandbox egress, not lockfile drift.
4. `DEPLOYMENT.md` retracts the "17 applied / nothing pending" claim and declines to invent a
   replacement, recording the one command that establishes the real state.

**The self-test is honest.** 17/17 pass. Mutation-tested: removing the no-env guard → 3 failures;
restoring the hardcoded `0009` apply → 4 failures. It can go red, so its green means something.

**FAIL — gate item 2 is not closed, and must not be closed on PR #89.** §4 gives it as `E00 + E01`,
"verify only when both merged". `.github/workflows/` holds `ci.yml` only; **there is no
`deploy.yml`**. `docs/ci/deploy-api.yml` was added by `c41a608` on 2026-07-31 and is still in
`docs/` two days later — PR #46's exact shape, still live. Every production deploy remains
hand-run. Not E01's fault: `DEPLOYMENT.md` opens with the `git mv` and names E00 as human-only.
E01's half is complete; the item's effect has not happened.

## Finding — new drift, same class as the defect E01 fixed

`docs/DEPLOYMENT.md` says **"`migrations/` contains 19 migrations"** (`:124`, also `:94`, `:135`).
`main` has **22** — `0020` (E06), `0021` (E16), `0022` (E13) merged after E01. The original defect
was a hardcoded migration count in prose going stale (17 vs 19); the replacement hardcodes a new
count in three places and went stale within a day. `DEPLOYMENT.md` is **E01's** `owns:`, so it was
not edited here. Drop the number or derive it from the `migrations list` command the file already
documents.

## Still owed

- **The four open PRs (#100, #101, #102, #103) need merging — the entire board is blocked on it.**
  Nothing in rounds 5 and 6 can start until then, and that is E00's standing duty.
- The `index.ts` freeze exemption for gate item 14 — **E00** (V02's finding, unchanged).
- A typecheck of `main` by someone with a working npm registry: twelve tasks merged, branch
  protection off, no required check has ever run against `main`.
- **Nine merged tasks are still unverified:** E03, E05, E06, E11, E13, E16, E17, E18, E20.
