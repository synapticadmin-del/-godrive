---
task: E05
title: Client truth fixes — disable سفر chip, SOS dark mode, delete the false SOS claim
status: done
claimed_by: chat-20260801-1832-1e6f
claimed_at_utc: 2026-08-01T18:33:40Z
branch: exec/05-client-truth-fixes
pr: https://github.com/synapticadmin-del/-godrive/pull/88
owns:
  - apps/rider/lib/screens/home/vehicle_selector.dart
  - apps/rider/lib/screens/safety/sos_screen.dart
migration: no
finished_at_utc: 2026-08-01T18:42:30Z
---

## Progress
- claimed
- read both owned files on main at f480905; re-verified all four plan citations (vehicle_selector.dart:33-54, sos_screen.dart:31,33,116, home_screen.dart:85) — all four accurate
- suspended the intercity chip with the file's existing mechanism (enabled: false → dimmed, "قريباً", null onTap); traced home_screen.dart read-only to prove onChanged is the only writer of `_category = 'intercity'`, so no file outside owns was needed
- moved the SOS screen onto AppTokens.sosBackdrop + transparent AppBar (parity with captain sos_screen.dart:117-122) and put the confirm dialog on GoTheme instead of lightPanel/lightText
- replaced the "سيتم إرسال موقعك للسلطات" copy with what safety.ts:15-51 actually does — an sos_alerts row plus a push to admin users — and said plainly that the app does not call 122/123
- PR #88 opened into main, +54 −8 across two files, both inside owns. Not merged, findings NOT closed.

## Status for the verifier
- Findings **S-030 (F-20-01)**, **S-108 (F-12-03)**, **S-028 (F-17-06)** are `awaiting-verification`.
- **Gate items 3 and 10 are both only partly delivered here — do not close either on PR #88.**
  Item 3 also needs E04 (server-side cash-only, unmount /intercity + /companies) and E02 (the mounts).
  Item 10 also needs E13 + E09 + E14; WAVE-PLAN §4 omits E05 from that split, so it takes **four** merges.
- Reproductions to run against main after merge are in the PR body under "How to verify the effect".

## Seam found (13th; §7 of WAVE-PLAN closed 12)
Flutter tests have no owner. `apps/rider/test/` is in no task's `owns` (E19 is `apps/api/test/` only), the E05
brief names no tests, and `.github/workflows/ci.yml:100-192` runs `flutter analyze` but never `flutter test`.
Not reached into. Proposed CI step + test file are in PR #88 for a human, alongside E00's workflow install.

## Notes
- No migration taken. MIGRATION-LOCK untouched; next free number is still 0020.
- Claims re-read immediately before pushing (§3.5): E01 had widened its owns to add `package-lock.json`; no
  intersection with E05. No conflict with anyone.
- PROJECT.md block appended on main at 149271d (append before TRACK-ENTRIES:END, no conflict).
