---
task: E03
title: trips.ts structural pass — extract settlement and dispatch (pure refactor)
status: done
claimed_by: chat-20260801-2047-c6b9
claimed_at_utc: 2026-08-01T20:56:00Z
branch: exec/03-trips-structural-pass
pr: https://github.com/synapticadmin-del/-godrive/pull/97
owns:
  - apps/api/src/routes/trips.ts
  - apps/api/src/lib/settlement.ts
  - apps/api/src/lib/dispatch.ts
migration: no
finished_at_utc: 2026-08-02T21:10:00Z
---

## Progress
- claimed. Dependency check: E02 is `done` (PR #94) **and** confirmed merged into `main` at
  `f58f227` — `apps/api/src/cron/{cleanup,dispatch,invoices,scheduled,types}.ts` and
  `apps/api/src/lib/health.ts` are all present on the base tree. LOCKED was empty at claim time
  (all 9 existing claims are `status: done`), so no `owns:` intersection.
- 2026-08-01T21:05Z — both brief line ranges opened and confirmed against `f58f227`, not trusted:
  settlement really is `985-1055`, dispatch really is `513-586`. E02 did not touch this file, so
  the `b0c0866` numbers still hold.
- 2026-08-01T21:08Z — re-read the claims folder before pushing (§3.5). **E12 was claimed at 21:02
  by `chat-20260801-2102-e03a` while this was in flight.** Its `owns:` (`middleware/requestId.ts`,
  `lib/log.ts`, `lib/audit.ts`, `lib/health.ts`) does not intersect mine. Safe; nothing merged over.
- 2026-08-01T21:10Z — done. PR #97 open, awaiting-verification.

## What I actually did

1. **Extracted the settlement block** (`trips.ts:985-1055`) into `lib/settlement.ts` behind a single
   exported `settleTripCompletion()`. One rename: `c.env.DB` → the `db` parameter. The mint bug —
   rider debit and captain credit not being one transaction — **moved across intact on purpose**;
   it is gate item 6 and belongs to E08, which owns this file next.
2. **Extracted the dispatch sequence** (`trips.ts:513-586`) into `lib/dispatch.ts` behind
   `dispatchTrip()`, plus `filterByCaptainRadius()` (`trips.ts:34-71`), whose only caller was that
   block. Renames only: `c.env`→`env`, the four `body.*` coordinates, `finalEstimate`,
   `est.fare.currency`, `user.id`, `createdEvent`→`pendingEvent`.
3. **Proved movement rather than modification** — the code was transformed mechanically from the
   source lines, then diffed back against the original with whitespace normalised and the renames
   applied: settlement 66/66 identical, dispatch 71/71 identical, `filterByCaptainRadius` identical.
   `trips.ts` 1371 → 1213 lines; no wallet SQL remains in it.
4. **`logEvent` is injected, not imported.** It has eight non-dispatch call sites in `trips.ts`, so
   importing it into `lib/` would duplicate the `trip_events` INSERT or create a `lib/→routes/`
   cycle. A shared writer belongs in `lib/tripEvents.ts` — in nobody's `owns:`, so I did not create it.
5. **No tests — the brief names none** (DoD #2). Flagged a real gap instead: E01 added
   `"test": "vitest run"` and vitest-pool-workers to `apps/api`, but `ci.yml`'s node job has **no
   `test @synaptic-go/api` step**, so an API test today runs nowhere. E19 will hit this. Proposed
   the YAML in the PR body for a human; §8 forbids me touching the workflow.

## Not touched

`cron/dispatch.ts` (E09's — `dispatchTrip()` is cron-callable but I did **not** wire the call site),
`index.ts` (frozen by E02), `lib/audit.ts` / `lib/health.ts` (E12's), `.github/workflows/**`,
`package-lock.json` (no dependency added). No migration, no l10n string.

## Gate items

**Closes none.** `gate_items: 6,7,9 (enabler)` — 6 is E08+E09, 7 is E09+E10, 9 is E09. A verifier
must not close any of them on PR #97; this task only makes them runnable in parallel.
