---
task: E16
title: Consent, account deletion and the legal surface
status: done
claimed_by: chat-20260801-1845-7a4c
claimed_at_utc: 2026-08-01T18:45:37Z
branch: exec/16-consent-deletion-legal
pr: https://github.com/synapticadmin-del/-godrive/pull/92
owns:
  - apps/api/src/routes/user.ts
  - migrations/@NEXT_consent_and_deletion.sql
  - docs/legal/
  - apps/rider/lib/screens/profile/settings_screen.dart
  - apps/captain/lib/screens/profile/settings_screen.dart
migration: yes — 0021, taken from MIGRATION-LOCK.md with its sha (E06 held 0020)
finished_at_utc: 2026-08-01T19:06:00Z
---

## Progress
- claimed
- LOCKED at claim time: E01, E06, E11 (all in_progress). E05 was done (PR #88). No intersection —
  E11 holds `apps/captain/lib/services/` and `screens/home/`, neither covering `screens/profile/`;
  E06 holds a different migration file. Every task below E16 was blocked on an unmerged dependency;
  E16 has `depends_on: [—]` and was the lowest-numbered eligible task.
- read all owned files on main at `149271d`; branched from main at `aac75bc` (only `PROJECT.md`
  differs between the two, so nothing owned moved underneath the work)
- took migration **0021** from MIGRATION-LOCK.md with its sha and bumped next-free to 0022
- wrote `0021_consent_and_deletion.sql`: append-only `user_consents` (UPDATE/DELETE blocked by
  `RAISE(ABORT)` triggers), `users.deleted_at`/`deletion_reason`, `account_deletion_requests`.
  Purely additive — no backfill, so not a fifth irreversible migration
- `user.ts`: `POST`/`GET /user/consent`, `GET /user/export` (15-table batch, credentials excluded),
  `DELETE /user/account` (soft delete + identifier tombstoning + R2 document deletion + refresh-token
  revocation), a `deleted_at` guard, and a new `publicUserRoutes` export carrying `GET`/`POST
  /user/deletion-request` for E02 to mount
- `docs/legal/`: policy in ar (authoritative) + en, account-deletion reference, retention and erasure
  runbook including the one-off R2 orphan reconciliation
- both apps: a privacy band (policy / export / delete) with a typed-confirmation deletion dialog that
  names what is *kept* as well as what goes
- PR #92 opened into main, 9 files, all inside owns. Not merged, findings NOT closed.

## Status for the verifier
- **F-25-01 (S-043)**, **F-25-02 (S-044)**, **F-25-03 (S-045)** and **T26 F-26-03** are
  `awaiting-verification`. **F-25-04 (S-157)** is only *partly* delivered here.
- **Do not close gate item 12 on PR #92.** It takes three parts: this PR, **E02**'s mount of
  `publicUserRoutes` in the frozen `index.ts`, and a **human** (counsel review, controller identity,
  the two store-listing URLs). WAVE-PLAN §4 lists items 6, 10 and 11 as the split ones and omits 12.
- Reproductions that name the *effect* are in the PR under "How to verify the effect".

## Seams found (§7 closed 12; E05 reported the 13th)
- **14th — `packages/flutter_shared/lib/l10n/app_strings.dart` has no owner.** New user-facing copy
  in both apps had nowhere to live; worked around with a private `_t(context, ar, en)` per screen.
  Same root as E05's 13th seam: the shared Flutter package is systematically unowned this wave.
- **15th — no owner for a global authenticated-user guard.** `middleware/auth.ts` is unowned, so a
  just-deleted user's stateless access token stays valid on `/trips`, `/captain` and `/wallet` for one
  token lifetime. Guarded `userRoutes` inside the owned file and revoked refresh tokens; documented
  rather than reached across.

## Notes
- Tests **not** added, and the brief's DoD cannot be met in round 1: `apps/api` has no harness on main
  (no `test/`, no `vitest.config.ts`, no `test` script), `apps/api/test/` is **E19**'s, and the
  devDependency is **E01**'s. The nine cases E19 should lift are written out in the PR body.
- Deployment ordering: migration 0021 must apply **before** the Worker deploys, or the `deleted_at`
  guard 500s every `/user/*` request. That ordering is what E01 fixes in `deploy.sh`.
- Claims re-read immediately before opening the PR (§3.5): E01, E05, E06, E11 all `done`, no
  intersection with this task's owns. No conflict with anyone.
