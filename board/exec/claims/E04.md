---
task: E04
title: Enforce the launch shape server-side — cash-only, verticals off
status: done
claimed_by: chat-20260802-0504-25c6
claimed_at_utc: 2026-08-02T05:05:10Z
branch: exec/04-launch-shape-server-side
pr: https://github.com/synapticadmin-del/-godrive/pull/100
owns:
  - apps/api/src/lib/schemas.ts
  - apps/api/src/routes/payments.ts
  - apps/api/src/routes/intercity.ts
  - apps/api/src/routes/companies.ts
migration: no
finished_at_utc: 2026-08-02T05:19:30Z
---

## Progress
- claimed
- LOCKED at claim time: E14 only (chat-20260801-2003-b678, claimed 2026-08-02T05:03Z) —
  apps/api/src/routes/admin.ts, apps/admin/. No intersection with this task's owns.
- Eligibility: depends_on [E03] is done AND merged (PR #97, verified merged=true via the pulls API).
  E00 is human-only. E01/E02/E03/E05/E06/E11/E12/E13/E16/E17/E18/E20 are done. E14 is in progress.
  E07 blocks on E04 (this task) and E09/E10/E19 block on E08 or E15, which are unstarted.
  E04 is the lowest-numbered eligible task.
- base main pinned at c0ce1740288f7b330bd5fbb5430c105e1cf0fd6e; branch cut from it, behind_by 0.
- 2026-08-02T05:18Z — PR #100 open, 4 files, +134 −2. PROJECT.md block appended to main at bcd1a13.
- status: awaiting-verification. Not merged by me, no finding closed by me.

## What I actually did

1. **Measured the defects on pristine main first.** Compiled and ran the tree at c0ce174:
   POST /trips {paymentMethod:"wallet"} returned 201 and executed INSERT INTO trips with
   payment_method="wallet" bound at arg 15 (same for "card"); POST /paymob/intention returned 200
   and wrote two rows for all three purposes — with no PAYMOB_* configured, because the provider
   client takes its `stubbed` path, so the descoped surface is not just present, it works.
2. **schemas.ts:** createTripSchema.paymentMethod narrowed to ["cash"]. parseBody runs before the
   handler (trips.ts:322), so a rejected method never reaches the INSERT at :431.
3. **payments.ts:** POST /paymob/intention refuses 403 PAYMENTS_DISABLED before parsing a body,
   contacting Paymob or writing a row; refusal ignores `purpose`, so all three are off. Narrowed
   intentionSchema behind it as an independent second lock.
4. **intercity.ts / companies.ts:** every path refuses 501 VERTICAL_DISABLED if the router is ever
   remounted. In companies.ts the guard is registered ahead of the file's existing
   use("*", authMiddleware), so a remount gives 501 and not the 401 that invites a credentialed retry.
   index.ts still answers 404 to the public and is untouched (E02 owns it).
5. **Guards written as `const X_ENABLED: boolean = false`,** not deletions or early returns — the
   annotation stops TS inferring the literal `false`, so the disabled bodies stay reachable to the
   type checker instead of rotting behind dead code, and re-enabling is one named line.

## Verification

- npm is firewalled in the sandbox (registry.npmjs.org 403); toolchain assembled from jsdelivr/esm.sh
  (hono 4.12.31, zod 3.25.76, jose 6.2.4 bundles; 316 .d.ts + TypeScript 5.9.3 lib.*.d.ts).
- **Behaviour: the same 25 assertions hold 4/25 on pristine main and 25/25 on this branch.**
  The DB stub answers the pricing_rules lookup deliberately — without it the baseline died at
  NO_PRICING and "creates nothing" passed for the wrong reason.
- **Types: tsc --noEmit with the repo's own tsconfig (strict) — 0 diagnostics on this branch,
  0 on pristine main (control), 1 TS2769 on a deliberate mutation (proof the checker checks).**

## Flags for the verifier

- **The brief's third acceptance criterion is NOT met.** "A test asserts each rejection, so
  re-enabling is a deliberate act that turns a test red" — the assertions pass, but they run in a
  sandbox and nothing in the repo turns red if the enum is widened back. apps/api/test/ and
  vitest.config.ts are E19's owns and do not exist on main; apps/api/package.json is E01's; ci.yml
  runs only `npm test -w @synaptic-go/shared`. Harness is in the PR body for E19 to lift. **Third
  independent report of this seam after E06 and E16** — first time it blocks an acceptance criterion.
- **Gate item 3 is the LAST of three, not another partial.** E02 (#94) and E05 (#88) are merged; this
  is the server half. Item 3 becomes closable when #100 merges, provided all three effects are
  checked. WAVE-PLAN §4's row still reads `E04 + E05` and should read `E02 + E04 + E05`.
- **S-005 (F-04-03)** and **S-069 (F-18-01)** → awaiting-verification, neutralised not repaired.
- **POST /paymob/webhook is still live on purpose** — pending intentions in production must still be
  able to settle; killing it would strand money Paymob has already taken.
- **intercityBookingSchema deliberately not narrowed** — the narrowing produces 2 × TS2367 at
  intercity.ts:138 and :182 (verified by mutation), which would mean rewriting handler bodies the
  launch shape freezes, for no gain: the vertical is unreachable at two layers.

## Not done, deliberately

- did not merge, did not close any finding
- no repair of anything downstream: the mint, the fare bypass and both verticals' defects are intact
- no file outside owns: trips.ts (E09), settlement.ts (E08), index.ts (E02, frozen), wallet.ts (E06),
  admin.ts (E14), ci.yml, apps/api/test/ (E19) all untouched

## Claims re-read before pushing (§3.5)

E08 was claimed during this run by chat-20260802-0506-3804 (lib/settlement.ts, lib/money.ts);
E14 remains in progress. Neither intersects this task's owns. No one holds a file this PR touches.
