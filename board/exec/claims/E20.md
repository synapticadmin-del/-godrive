---
task: E20
title: Rider side of the location pipeline — wire the interpolating marker
status: done
claimed_by: chat-20260801-2009-727e
claimed_at_utc: 2026-08-01T20:09:36Z
branch: exec/20-rider-marker-wiring
pr: https://github.com/synapticadmin-del/-godrive/pull/95
owns:
  - apps/rider/lib/screens/trip/trip_screen.dart
  - apps/rider/lib/screens/home/home_screen.dart
migration: no
finished_at_utc: 2026-08-01T20:25:00Z
---

## Progress
- claimed
- LOCKED at claim time: **E02** (`apps/api/src/index.ts`, `apps/api/src/cron/`, `apps/api/src/lib/health.ts`),
  **E17** (two `build.gradle`, two `Info.plist`), **E18** (`scripts/backup-d1.sh`, `docs/RUNBOOK-restore.md`).
  None intersects this task's `owns`.
- Eligibility: `depends_on: [E11]`, and E11 is `done` **and** PR #91 is merged into `main`
  (pulls API: `merged=true`, `merge_commit_sha 0ae95897`) — the widget this task calls exists on
  `main`, not merely on a branch. Every lower-numbered task was blocked: E00 is human-only;
  E03/E04/E07/E08/E09/E10/E12/E13/E14/E15/E19 all chain onto E02 (in progress) or E03 (unstarted);
  E16 is `done` but PR #92 is still open. E20 was the only eligible task on the board.
- base `main` pinned at `417d35d7ff18b233bc27810a629b44665c5bb7ac`; branched from `f917932` after
  `main` moved during the run. Confirmed via the compare API that the only file changed between the
  two was `PROJECT.md` (E17's block) — nothing this task owns moved underneath the work.
- migration: **no**. `MIGRATION-LOCK.md` not touched; next free number stays 0024.
- 2026-08-01T20:25Z — done. PR #95 open, `PROJECT.md` block appended to `main` at `1f24d62`.

## What I actually did

1. **Re-verified all three cited defects on `main` rather than trusting the brief** — `trip_screen.dart:145-151`
   (hard assignment), `:383-391` (no heading argument anywhere in the call) and `home_screen.dart:618-634`
   plus `:223` (wholesale replace). All three accurate. Also established two facts the scope depends on:
   the socket **does** carry `heading` (`TripRoom.ts:222`, `captain.ts:305`) and the rider was discarding
   it, while the nearby payload does **not** (`CaptainPresence` is `{userId, lat, lng, lastSeen, name}`),
   so the home map has to derive its own bearing.
2. **`trip_screen.dart`:** the fix handler resolves a heading alongside the position — reported, else the
   bearing of the leg just travelled, else the heading already on screen — and the car moved into its own
   layer so `AnimatedVehicleMarker` can rebuild it per frame while the endpoint pins stay static and cheap.
   Same stacking as before (drawn above the pins). `fixInterval` deliberately not passed: on a live socket
   the widget's own clock is right.
3. **`home_screen.dart`:** `_nearbyCaptains` became identified records merged by `userId`, one keyed layer
   per captain. The key is the load-bearing part — unkeyed, poll two hands car A's animation state to
   whoever is now first in the list. The measured poll gap is passed as `fixInterval` so the widget's
   stale-gap rule applies honestly at the real 45 s cadence instead of animating one 45-second jump.
4. **Reduce-motion skips the tween entirely** on both screens — no controller, no per-frame rebuild — and
   the marker still tracks every fix and still points the right way.
5. **Proof:** 29/29 assertions executed against E11's real `go_motion.dart` and the real `latlong2` 0.9.1,
   covering jitter, held headings, `-1`/`NaN` rejection, stale gaps, tween continuity and short-way rotation.
   Both files parse-checked with `dart format` (control-tested against a deliberate syntax error). Every API
   verified against actual resolved package source: `flutter_map` 6.2.1 `Marker.key`, `latlong2` 0.9.1
   `Distance.bearing`, and E11's own builder→`MarkerLayer` idiom already shipping at `main_shell.dart:636-656`.

## Status for the verifier

- **Do not close gate item 8 on this PR.** It is split between `E11` (PR #91, merged) and this PR, and
  closes only when both have merged **and** been verified. WAVE-PLAN §4 carries the row.
- **`flutter analyze` was not run** — no Flutter SDK in the sandbox. That is the one acceptance criterion
  I could not discharge and the first thing to run. The command CI uses is
  `flutter analyze --no-fatal-infos --no-fatal-warnings` in `apps/rider`.
- Reproductions that name the *effect* are in the PR body under "What a verifier should run on `main`".
- Not merged by me. No finding closed by me.

## Seams found (three; none reached across)

- **16th — `packages/flutter_shared/lib/flutter_shared.dart` does not export E11's two new files.** The
  barrel stops at `go_date_field.dart`, so every consumer must deep-import forever. It is in *nobody's*
  `owns` — not E11's (which names the two leaf files), not E16's (which names `l10n/app_strings.dart`).
  Two chats have now worked around the same missing two lines. Same root as the `app_strings.dart` gap in
  WAVE-PLAN §8. Not edited.
- **17th — the brief's claim that `flutter_shared` "already exposes the [reduce-motion] contract" is false.**
  No such helper exists in the package; the repo idiom is a raw
  `MediaQuery.maybeOf(context)?.disableAnimations ?? false` repeated per screen. Followed the idiom.
  Carry forward: **`AnimatedVehicleMarker` does not check it either**, so every caller must handle
  reduce-motion at the call site and a future one will forget. The check belongs in the widget (E11's file).
- **18th — the bearing derivation the brief requires has no home.** `go_motion.dart` has `metresBetween`,
  `goLerpHeading` and the jitter threshold but no bearing-between-two-points, and with no shared file inside
  this task's `owns` the ~10-line adapter exists in both screens. It is an adapter, not a second
  implementation (distance is `GoMotion.metresBetween`, trigonometry is `latlong2`'s, threshold is
  `GoMotion.jitterThresholdMetres`). It should collapse into a `goBearingBetween` in `go_motion.dart`.
  Creating a shared rider file instead would have put a new path in nobody's `owns`.

## Notes

- Claims re-read immediately before opening the PR (PROTOCOL-EXEC §3.5): E02, E17, E18 `in_progress`,
  none holding either of this task's two files. No conflict with anyone.
- Nothing outside `owns` was edited. `packages/flutter_shared/**` is E11's and both widgets are
  deep-imported exactly as the captain app already does, with the reason in a comment at the import site.
