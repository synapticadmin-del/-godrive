---
task: T05
title: Pricing, Surge & Bidding Economics
status: done
claimed_by: chat-20260801-1228-83a3
claimed_at_utc: 2026-08-01T12:28:22Z
branch: plan/05-pricing-surge-bidding-economics
doc: docs/plan/05-pricing-surge-bidding-economics.md
pr: https://github.com/synapticadmin-del/-godrive/pull/61
finished_at_utc: 2026-08-01T12:49:30Z
---

## Progress
- Reviewed the whole price surface at base commit `f906e0b06240ea84b20ef5c7b5633fd9abe02f8b`: `lib/pricing.ts`,
  `lib/routing.ts`, `packages/shared/src/index.ts` + its tests, `lib/schemas.ts`, `middleware/rateLimit.ts`, the
  estimate/create/cancel/accept/complete/bid/bids/accept-bid handlers in `routes/trips.ts`, `routes/promo.ts`,
  `routes/captain.ts` (`/offers`), `routes/admin.ts` (pricing CRUD), `routes/intercity.ts`, migrations 0001-0005/0016,
  `PricingPage.tsx`, and all five Flutter price surfaces. 1061-line document, 35 findings (12 S1, 13 S2, 7 S3, 3 S4).
- **Headline S1: the rider's offer never becomes money.** `POST /trips/:id/accept` writes no price column
  (`trips.ts:861-866`), so settlement resolves to `estimated_fare` (`:969`) while the captain's accept button showed
  `offered_price` (`captain.ts:462-467`, `offer_card.dart:239-243`). Accept a 150 EGP offer on a 61 EGP estimate, get
  paid on 61, with no signal to either party. The bid path is self-consistent, which is why it survived review so far.
- Other S1s: promo budget has no floor (TOCTOU at `:418`/`:500` plus free create+cancel drain, `:709-827`); the captain
  funds ~80% of every discount (`:428-429`, `:969-971`); offers and counters bounded only at 1..10000 with no relation to
  the estimate (`schemas.ts:59,77`); bid spam with no dedup/TTL/rate limit; silent OSRM→haversine repricing at +28%
  (`routing.ts:61-62`); vehicle tiers never priced server-side so XL == economy; no cancellation/no-show fee though the
  admin config for one is live and dead; wallet trips complete on a failed debit (`:997-1010`).
- Two brief premises corrected by evidence and recorded in §1 so nobody repeats the search: `pricing_rules` and
  `vehicle_types` are in migrations 0001/0002, not 0003; and `pricing_rules.surge_multiplier` **does not exist** — the
  only surge column is on `trips`, which is exactly why the read at `trips.ts:390-392` is dead and surge is pinned to 1.0.
  Verified non-findings also recorded: the assignment race IS correctly serialised on both paths, wallet moves are
  idempotency-keyed, `commissionRate` IS bounded 0..1, and intercity price cannot be client-supplied.
- Deliverable includes the required manipulation matrix (13 client-supplied money fields), a signed-quote design
  (P0.1, HMAC quote echoed back at create, closing 5 matrix rows), and unit economics for an 8km Cairo trip
  (19.5% cash / 17.0% card contribution margin, confirmed repo values kept separate from assumed market rates).

## Handoffs
T27, T03, T04, T06, T02, T25, T08, T11, T17, T20, T22, T23 — detail in §9 of the doc.
