---
task: T02
title: Authorization, RBAC & Object-Level Access
status: done
claimed_by: chat-20260801-1214-a0bd
claimed_at_utc: 2026-08-01T12:14:20Z
branch: plan/02-authorization-rbac-idor
doc: docs/plan/02-authorization-rbac-idor.md
pr: https://github.com/synapticadmin-del/-godrive/pull/60
finished_at_utc: 2026-08-01T12:42:17Z
---

## Progress
- Delivered `docs/plan/02-authorization-rbac-idor.md` (933 lines) on branch
  `plan/02-authorization-rbac-idor`; PR #60 opened into `main`. PROJECT.md block appended.
- Full authorization matrix for all **117 endpoints** across the 14 route files —
  endpoint x {anonymous, rider, captain, admin, company} with the enforcing `path:line`.
  Note there is no `company` role: `users.role` is a 3-value CHECK, so a "company employee"
  is a rider with an active `company_employees` row.
- 20 findings: 3 S1, 7 S2, 8 S3, 2 S4. The S1s are `POST /trips/estimate` running
  unauthenticated and leaking every online captain's id/name/distance (`trips.ts:315` above
  the guard at `:346`); the Paymob intention accepting a client-chosen `amount` and an
  unbound `tripId`, letting any trip settle for 1 EGP (`payments.ts:13`/`:74`/`:200-205`);
  and trip sharing being wholly non-functional (`safety.ts:11` covers `:90`, and the URL at
  `:83` omits the `/safety` prefix) while still reporting success to the rider.
- Three routing claims verified by executing real Hono 4.7.11, not by reading source. One
  **overturned a finding I had already drafted as an S1**: `use("/admin/*")` does match the
  bare `/admin` path, so `companies.ts` is not a cross-tenant breach. 12 verified
  non-findings are recorded in the document's section 4.1 so they are not re-raised.
- Nothing outside my own files was touched: no product code, no workflows, no other track's
  claim or PROJECT.md block, no merges or closes.
