---
task: T08
title: Data Model, Migrations & Integrity
status: done
claimed_by: chat-20260801-1214-a0bd
claimed_at_utc: 2026-08-01T13:22:00Z
branch: plan/08-data-model-migrations-integrity
doc: docs/plan/08-data-model-migrations-integrity.md
pr: https://github.com/synapticadmin-del/-godrive/pull/69
finished_at_utc: 2026-08-01T13:45:50Z
---

## Progress
- claimed; same CHAT_ID as T02 (PR #60). The first attempt died mid-deliverable on an HTTP 402
  before any branch existed; resumed in a fresh container and re-derived every piece of evidence
  from scratch against pinned base commit 697f4347. Nothing from the lost run was carried over
  unverified.
- 20 findings: 5 S1, 8 S2, 5 S3, 2 S4. Deliverable is 1,204 lines and includes the consolidated
  schema for all 36 tables, which did not previously exist and is generated from the applied
  database rather than hand-written.
- The two headline S1s are both B2B invoicing, wrong in opposite directions for the same reason:
  the cron issues ~1,440 invoices/month (discards event.cron, then its SUM and its settling UPDATE
  disagree on timestamp format), while a second generator at companies.ts:167 silently under-bills.
  Both reproduced in SQLite. Root cause is two timestamp producers writing two formats into the
  same TEXT columns.
- Other S1s: four highest-volume tables have no retention against D1's unraisable 10 GB cap
  (breached in ~166 days at the 10k trips/day benchmark), and there is no backup beyond an
  unrehearsed platform default. Verified D1 *does* enforce foreign keys by default, which corrects
  a note left on T04.
- One claim was dropped rather than shipped: an early scan suggested 6 of 8 wallet writes lacked a
  balance guard; re-checked across multi-line statements, all three debits are guarded. D1 per-file
  migration atomicity is left as needs-check.
