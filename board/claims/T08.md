---
task: T08
title: Data Model, Migrations & Integrity
status: in_progress
claimed_by: chat-20260801-1214-a0bd
claimed_at_utc: 2026-08-01T13:22:00Z
branch: plan/08-data-model-migrations-integrity
doc: docs/plan/08-data-model-migrations-integrity.md
pr:
finished_at_utc:
---

## Progress
- claimed
- Same chat that completed T02 (PR #60), continuing at the operator's instruction.
  Not a second identity: the CHAT_ID is deliberately reused so the board shows one
  chat holding both tracks.
- 13:22Z — the run was killed by a billing/infrastructure error (HTTP 402) mid-deliverable,
  before any branch or document existed, and has resumed in a fresh container at the
  operator's instruction. `claimed_at_utc` refreshed because the lock is genuinely live
  again, not to extend a dead one. Same CHAT_ID; no takeover has occurred and no other
  chat's claim was touched. The previous container's downloads and draft are gone, so all
  evidence is being re-derived from scratch against pinned base commit 697f4347; nothing
  from the lost run is carried into the document unverified.
