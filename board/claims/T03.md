---
task: T03
title: Money Integrity — Wallet, Ledger & Commission
status: done
claimed_by: chat-20260801-1219-a7e2
claimed_at_utc: 2026-08-01T12:19:30Z
branch: plan/03-money-integrity-wallet-ledger
doc: docs/plan/03-money-integrity-wallet-ledger.md
pr: https://github.com/synapticadmin-del/-godrive/pull/63
finished_at_utc: 2026-08-01T12:50:30Z
---

## Progress
- Claimed, verified by read-back, then pinned base commit `dccc2dadf206ffaba4d35b3343d55ff279bacaf4` and used it as the ref for every citation in the document.
- Read the money surface directly: `wallet.ts` in full, `trips.ts` settlement/cancel/bid-accept, `payments.ts`, `intercity.ts`, `promo.ts`, `admin.ts` analytics, `captain.ts` earnings, lib (pricing, utils, schemas, paymob, audit, cleanup), 10 migrations, both Flutter wallet/earnings screens and `AnalyticsPage.tsx`. Four subagents ran the PSP, refund/promo, client-surface and reconciliation axes in parallel; every S1 and every load-bearing S2 was re-verified by hand before it was written down.
- Findings: 9 S1, 15 S2, 8 S3, 2 S4. Headline — the captain is credited in full when the rider's wallet debit fails (`trips.ts:1003` + `:1012`); Paymob top-ups credit the balance on every webhook replay (`payments.ts:175-186`); card-paid intercity seats are never charged; the intercity webhook inserts a CHECK-violating `type` and throws on every retry after the card is captured; there is no refund path for city trips at all; and the ledger is single-sided with no platform account, so it cannot balance by construction.
- Deliverable: `docs/plan/03-money-integrity-wallet-ledger.md`, 774 lines, TEMPLATE sections 1-10, including an 18-invariant reconciliation suite with runnable SQL verified against a schema rebuilt from `migrations/`. PR #63 opened into `main`.
- `PROJECT.md` block appended immediately before the sentinel on `main`. One 409 conflict was hit and handled by re-reading (T04 and T05 had landed mid-run); T01/T02/T04/T05 entries verified preserved byte-for-byte.
