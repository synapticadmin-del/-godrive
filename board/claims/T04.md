---
task: T04
title: Payments, PSP Integration & Captain Payouts
status: done
claimed_by: chat-20260801-1226-ad01
claimed_at_utc: 2026-08-01T12:26:25Z
branch: plan/04-payments-psp-payouts
doc: docs/plan/04-payments-psp-payouts.md
pr: https://github.com/synapticadmin-del/-godrive/pull/62
finished_at_utc: 2026-08-01T12:48:33Z
---

## Progress
- claimed 12:26Z; base commit of `main` pinned for every citation: `dccc2dadf206ffaba4d35b3343d55ff279bacaf4`.
- Read `lib/paymob.ts`, `routes/payments.ts`, `routes/wallet.ts`, migrations 0003/0005/0011 and the
  `trips.ts` completion handler line by line; four subagents covered the ledger, Flutter, admin and
  schema/absence axes. Every `path:line` was re-verified by hand before it went in the document.
- 26 findings: **6 S1**, 11 S2, 8 S3, 1 S4, plus one positive (PCI scope is correctly avoided).
  Headline S1s: the webhook's balance `UPDATE` ignores its own `INSERT OR IGNORE` result and settles
  the intention 40 lines later, so a retry double-credits (`payments.ts:175-186`, `:223`);
  `purpose='intercity_booking'` violates the `wallet_transactions.type` CHECK so every such payment
  500s forever (`payments.ts:217` vs `0003:30`); and `POST /captain/wallet/payout` debits a captain's
  balance into a `pending` row nothing can ever action (`wallet.ts:113-130`).
- Corrected two things a first pass got wrong, both checked directly against the schema:
  `trips.payment_status` **does** exist (added by `0011:31`, not `0001`), so the webhook's UPDATE is
  valid SQL; and captain payouts are not "entirely missing" as the brief assumed — the endpoint
  exists, is wired to a live button, and takes money. Worse than missing, so it is filed as an S1.
- Doc merged as 884 lines at `docs/plan/04-payments-psp-payouts.md`; PR #62 opened against `main`;
  `PROJECT.md` block appended before the sentinel on `main` (no conflict, T01/T02 entries preserved).
  Cross-cutting notes handed to T02, T03, T08, T19, T20, T21, T22, T25 and T27. No product code touched.
