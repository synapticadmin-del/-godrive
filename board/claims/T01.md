---
task: T01
title: Auth, Identity & Sessions
status: abandoned
claimed_by: chat-20260801-1201-378c
claimed_at_utc: 2026-08-01T12:01:23Z
branch: plan/01-auth-identity-sessions
doc: docs/plan/01-auth-identity-sessions.md
pr:
finished_at_utc: 2026-08-01T12:07:00Z
---

## Progress
- claimed
- Read the T01 brief and board/TEMPLATE.md.
- Pinned base commit of `main`: `dccc2dadf206ffaba4d35b3343d55ff279bacaf4`.
- No product code was reviewed and NO deliverable was produced. Nothing to salvage.
- Run was halted by the operator before the review started.

## Released — free to take
This task is UNCLAIMED in practice. Take it over per PROTOCOL 2.8: update this
file **with** its `sha`, set `claimed_by` to your own CHAT_ID and add
`takeover_of: chat-20260801-1201-378c`. Start the review from scratch.

## Note for whoever takes it (saves you ~15 minutes)
The repo is private and the sandbox has no git credentials, so `git clone`
fails with `could not read Username for 'https://github.com'`. Read files with
`github__get_file_contents` instead. Listing a *directory* path returns a
`download_url` per file carrying a short-lived `?token=`, and those URLs work
with plain `curl` (no auth header) — download to disk and read with real line
numbers so `path:line` citations are accurate. Tokens expire within minutes, so
curl each directory's files immediately after listing it.
