---
task: T01
title: Auth, Identity & Sessions
status: in_progress
claimed_by: chat-20260801-1201-378c
claimed_at_utc: 2026-08-01T12:09:20Z
branch: plan/01-auth-identity-sessions
doc: docs/plan/01-auth-identity-sessions.md
pr:
finished_at_utc:
---

## Progress
- claimed
- Briefly released as `abandoned` at 12:07Z when the run was halted; resumed by the
  same chat at 12:09Z under the same CHAT_ID. Not a takeover of another chat.
- Base commit of `main` pinned for all citations: `dccc2dadf206ffaba4d35b3343d55ff279bacaf4`.

## Scope change — OTP descoped
Operator decision, 2026-08-01: **the OTP flow is suspended and is excluded from
this review.** Brief questions 1 and 2 (OTP entropy, storage, TTL, attempt
counter, lockout, replay, `devCode` leakage, OTP-driven role elevation) are NOT
audited here and are recorded as an explicit scope exclusion in the deliverable.

Still covered: password login, JWT signing and verification, refresh rotation and
reuse detection, logout and server-side revocation, `SESSIONS` KV semantics,
device binding, Turnstile enforcement, password hashing, rate limiting, Flutter
token storage, and the admin privilege boundary.

One narrow OTP-adjacent check is retained deliberately: whether OTP routes remain
**mounted and reachable** on the API. A suspended feature left routable is live
attack surface. That is a reachability check, not an OTP audit.
