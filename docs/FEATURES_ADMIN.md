# فيتشرز لوحة تحكم الإدارة (Admin)

> **النطاق:** لوحة تحكم React — `apps/admin/`
> **المصدر:** قراءة مباشرة من الكود (admin routes + صفحات React)
> **آخر تحديث:** 2026-08-01

---

## 1. لوحة القيادة (Dashboard)

| الفيتشر | الوصف | المصدر |
|---------|-------|--------|
| إحصائيات المستخدمين | عدد العملاء/الكباتن/الأدمن | `admin.ts` `/stats` |
| الرحلات حسب الحالة | توزيع searching/offered/assigned/completed/cancelled | `admin.ts` |
| مؤشرات اليوم | رحلات اليوم + GMV + عمولة | `admin.ts` |
| الكباتن أونلاين | عدد المعتمدين المتصلين الآن | `admin.ts` |
| طلبات التحقق المعلقة | عدد الكباتن بانتظار المراجعة | `admin.ts` |

---

## 2. التحليلات (Analytics)

| الفيتشر | الوصف | المصدر |
|---------|-------|--------|
| الرحلات اليومية | رسم بياني للرحلات والإيرادات بمرور الوقت | `admin.ts` `/analytics` |
| مقارنة بالفترة السابقة | نسبة التغير في الرحلات/الإيراد/الإتمام | `admin.ts` |
| أفضل الكباتن | Top 10 بالرحلات والإيرادات | `admin.ts` |
| معدل الإتمام | نسبة الرحلات المكتملة من الإجمالي | `admin.ts` |
| فلترة بالتاريخ | from/to مع date-only bounds | `admin.ts` |

---

## 3. إدارة الكباتن

| الفيتشر | الوصف | المصدر |
|---------|-------|--------|
| قائمة الكباتن | كل البيانات + المركبة + التقييم + الحالة | `admin.ts` `/captains` |
| فلترة بالحالة | pending/approved/suspended | `admin.ts` |
| اعتماد كابتن | يفعّل الحساب + يسمح بالعمل | `admin.ts` `/approve` |
| تعليق كابتن | يوقف الحساب + يخرجه offline | `admin.ts` `/suspend` |
| تنظيف الجلسات القديمة | offline تلقائي بعد 5 دقائق بدون heartbeat | `admin.ts` |
| الكباتن أونلاين الآن | مواقعهم الحية على الخريطة | `admin.ts` `/online-captains` |

---

## 4. التحقق من المستندات

| الفيتشر | الوصف | المصدر |
|---------|-------|--------|
| قائمة المستندات | مجمعة بالكابتن — لا تقسيم لملف واحد | `admin.ts` `/documents` |
| فلترة بالحالة | pending/approved/rejected | `admin.ts` |
| عرض المستند | صورة/PDF من R2 مع auth | `admin.ts` `/documents/:id/file` |
| اعتماد مستند | يحدّث الحالة — يعتمد الكابتن تلقائيًا عند اكتمال الكل | `admin.ts` `/review` |
| رفض مستند | مع سبب إلزامي يظهر للكابتن | `admin.ts` `/reject` |
| إدارة أنواع المستندات | إضافة/تعديل/إلغاء تفعيل — بدون نشر تطبيق | `admin.ts` `/document-types` |

---

## 5. إدارة الرحلات

| الفيتشر | الوصف | المصدر |
|---------|-------|--------|
| كل الرحلات | آخر 200 رحلة مع فلترة بالحالة | `admin.ts` `/trips` |
| الرحلات النشطة | live-trips — كل ما هو ليس مكتمل/ملغي | `admin.ts` |
| تفاصيل الرحلة | المسار + الأحداث + الطرفان | `trips.ts` `GET /:id` |
| إلغاء رحلة | إجباري من الأدمن مع سبب | `trips.ts` `/cancel` |
| إتمام رحلة | إجباري عند عطل — مع نفس منطق المحفظة | `trips.ts` `/complete` |

---

## 6. التسعير والإعدادات

| الفيتشر | الوصف | المصدر |
|---------|-------|--------|
| قواعد التسعير | لكل مدينة: base/per_km/per_min/booking_fee/min_fare/commission | `admin.ts` `/pricing` |
| تحديث التسعير | تعديل جزئي مع audit log | `admin.ts` `PUT /pricing/:city` |
| أنواع المركبات | الفئات والمضاعفات (economy/comfort/xl) | `admin.ts` `/vehicle-types` |
| إعدادات المنصة | عمولة افتراضية، نطاق البحث، إلغاء مجاني، غرامة إلغاء | `admin.ts` `/system-config` |
| أرقام الدعم | هاتف وواتساب — تظهر في التطبيقات | `admin.ts` |
| Auto-assign | تفعيل/تعطيل التعيين التلقائي | `admin.ts` |

---

## 7. البروموكودات

| الفيتشر | الوصف | المصدر |
|---------|-------|--------|
| قائمة البروموكودات | كل الأكواد مع الاستخدام والانتهاء | `promo.ts` `GET /` |
| إنشاء بروموكود | نسبة أو مبلغ ثابت + حد استخدام + انتهاء | `promo.ts` `POST /` |
| إلغاء تفعيل | soft delete — يبقى في السجل | `promo.ts` `/deactivate` |

---

## 8. الشركات (B2B)

| الفيتشر | الوصف | المصدر |
|---------|-------|--------|
| قائمة الشركات | كل الشركات المسجلة | `companies.ts` `/admin` |
| إنشاء شركة | اسم قانوني + ضريبة + حد ائتمان | `companies.ts` |
| إضافة موظف | ربط مستخدم + حد إنفاق شهري + أنواع مركبات مسموحة | `companies.ts` |
| تعديل موظف | تفعيل/تعطيل + تعديل الحدود | `companies.ts` |
| فاتورة شهرية | تجميع رحلات الشهر + إصدار فاتورة | `companies.ts` `/invoice` |
| سجل الفواتير | كل الفواتير السابقة | `companies.ts` |
| بوابة الشركة | view محدود للشركة نفسها (invoices + trips) | `companies.ts` `/portal` |

---

## 9. الرحلات بين المحافظات

| الفيتشر | الوصف | المصدر |
|---------|-------|--------|
| إدارة المسارات | إضافة/تعديل خطوط المدن | `intercity.ts` `/admin/routes` |
| إدارة الجداول | مواعيد المغادرة والمقاعد | `intercity.ts` `/admin/schedules` |
| تعيين كابتن | لكل جدول — مع إشعار FCM | `intercity.ts` `/assign` |

---

## 10. البحث والمراقبة

| الفيتشر | الوصف | المصدر |
|---------|-------|--------|
| بحث سريع | كباتن/عملاء/رحلات — LIKE بحرفين+ | `admin.ts` `/search` |
| سجل التدقيق | كل الأحداث الإدارية (audit log) | `admin.ts` `/audit-log` |
| خريطة حية | الرحلات النشطة + الكباتن أونلاين | `LiveMapPage.tsx` |

---

## 11. الأمان والوصول

| الفيتشر | الوصف | المصدر |
|---------|-------|--------|
| تسجيل دخول أدمن | إيميل + كلمة مرور فقط (لا OTP) | `auth.ts` `/login` |
| إعداد أدمن أول مرة | `/admin/setup` — مرة واحدة فقط مع setup secret | `auth.ts` |
| JWT قصير المدى | access token + refresh rotation | `auth.ts` |
| CORS محدود | نطاقات محددة فقط — لا *.pages.dev | `index.ts` |
| Rate limiting | عام 120/دقيقة + خاص بكل endpoint | `rateLimit.ts` |

---

## ملخص الفيتشرز

| الفئة | العدد | الحالة |
|-------|-------|--------|
| لوحة القيادة | 5 | ✅ مكتمل |
| التحليلات | 5 | ✅ مكتمل |
| إدارة الكباتن | 6 | ✅ مكتمل |
| التحقق من المستندات | 6 | ✅ مكتمل |
| إدارة الرحلات | 5 | ✅ مكتمل |
| التسعير والإعدادات | 6 | ✅ مكتمل |
| البروموكودات | 3 | ✅ مكتمل |
| الشركات B2B | 7 | ✅ مكتمل |
| Intercity | 3 | ✅ مكتمل |
| البحث والمراقبة | 3 | ✅ مكتمل |
| الأمان والوصول | 5 | ✅ مكتمل |
| **المجموع** | **54** | **مكتمل** |

---

> **ملاحظة:** كل فيتشر مذكور هنا مستخرج من الكود الفعلي في `apps/api/src/routes/admin.ts` و`apps/admin/src/pages/`. لا توجد فيتشرز مخططة — فقط ما هو منفَّذ ويعمل.
